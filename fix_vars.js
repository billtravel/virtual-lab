const fs = require('fs'); const file = 'src/content/topics/p201/Animation.jsx'; let code = fs.readFileSync(file, 'utf8'); const vars = \\n  const panelClass = embedded ? 'bg-white/10 backdrop-blur-xl rounded-2xl border border-white/15 shadow-xl' : 'bg-gray-900/60 backdrop-blur-xl rounded-2xl border border-gray-700/50 shadow-2xl';\n  const subPanelClass = embedded ? 'bg-white/5 backdrop-blur-sm rounded-2xl border border-white/10 shadow-[0_8px_32px_rgba(0,0,0,0.2)]' : 'bg-gray-800/40 backdrop-blur-sm rounded-2xl border border-gray-700/50 shadow-[0_8px_32px_rgba(0,0,0,0.2)]';\n\; code = code.replace(/const TuningForkExperiment = \\(\\{ embedded \\}\\) => \\{/g, 'const TuningForkExperiment = ({ embedded }) => {' + vars); code = code.replace(/const VacuumExperiment = \\(\\{ embedded \\}\\) => \\{/g, 'const VacuumExperiment = ({ embedded }) => {' + vars); code = code.replace(/const SpeedSoundExperiment = \\(\\{ embedded \\}\\) => \\{/g, 'const SpeedSoundExperiment = ({ embedded }) => {' + vars); fs.writeFileSync(file, code);
